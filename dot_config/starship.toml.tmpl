{{- /*
  Which prompt gets deployed.

  "terminal-stack" (the default, and everything below this block) is this repo's
  own two-line prompt. Any other value is one of Starship's twelve built-in
  presets, emitted by `starship preset <name>` right here at apply time rather
  than vendored -- vendoring twelve TOML files would freeze them at whatever
  upstream shipped the day they were copied, and the whole point of a preset is
  that it is Starship's, not ours.

  `lookPath` is the load-bearing half. During a bootstrap this template can be
  rendered BEFORE starship is installed, and `output` on a missing binary aborts
  the entire apply -- not just this file. Falling back to our own config keeps a
  half-installed machine with a working prompt, and the next apply picks up the
  preset.

  Set it with `tstack config prompt <name>`, which lists what is available and
  can show you each one rendered before you choose.
*/ -}}
{{- $preset := index . "starshipPreset" | default "terminal-stack" -}}
{{- if and (ne $preset "terminal-stack") (lookPath "starship") -}}
# Starship's built-in "{{ $preset }}" preset, rendered by `starship preset` at
# apply time. Managed by terminal-stack: edits here are replaced on the next
# apply. To go back to this stack's own prompt: tstack config prompt terminal-stack
{{ output "starship" "preset" $preset }}
{{- else -}}
"$schema" = "https://starship.rs/config-schema.json"

# Powerlevel10k Rainbow-style two-line prompt rebuilt in Starship.
# Reference: context/bestprompt.jpg (reference screenshot, 2026-05).
# Top line: ╭─ os · cwd · git-branch · git-status · ……… · user@host · clock · ─╮
# Bottom line: ╰─ ❯
#
# Glyph requirements: any Nerd Font (JetBrainsMono Nerd Font is the stack default).
# Nerd Font glyphs use \u escapes — the Private-Use-Area characters (U+E000–U+F8FF)
# are not safe to paste literally because some editors silently strip them.
#
# Colours come from the `palette` selected below (Catppuccin Mocha = dark,
# VS Code Light Modern = light). The palette overrides the standard colour names
# used in the styles, so the prompt tracks the stack's theme. `palette` is baked at apply
# time from the saved config (themeMode/resolvedTheme); change it with
# `tstack config theme <dark|light|follow>`.
palette = "{{ if hasKey . "resolvedTheme" }}{{ .resolvedTheme }}{{ else }}dark{{ end }}"

add_newline      = true
command_timeout  = 1000

format = """
[╭─ ](bold black)\
$os\
$directory\
$git_branch\
$git_status\
$cmd_duration\
$fill\
$username\
[@](bold black)\
$hostname\
$time\
[ ─╮](bold black)
[╰─](bold blue)$character"""

# ── Prompt arrow (line 2) ─────────────────────────────────────────────────────
[character]
success_symbol = "[❯](bold blue)"
error_symbol   = "[❯](bold red)"
vimcmd_symbol  = "[❮](bold blue)"

# ── OS icon (distro/platform logo, Nerd Font) ─────────────────────────────────
[os]
disabled = false
format   = "[$symbol ](bold blue)"

[os.symbols]
Alpine           = "\uf300"
Arch             = "\uf303"
CentOS           = "\uf304"
Debian           = "\uf306"
Fedora           = "\uf30a"
Linux            = "\uf17c"
Macos            = "\uf179"
Manjaro          = "\uf312"
Mint             = "\uf30e"
NixOS            = "\uf313"
openSUSE         = "\uf314"
Pop              = "\uf32a"
Raspbian         = "\uf315"
Redhat           = "\uf316"
RedHatEnterprise = "\uf316"
Ubuntu           = "\uf31b"
Unknown          = "\uf059"
Windows          = "\uf17a"

# ── Current directory (folder glyph + path) ───────────────────────────────────
[directory]
format            = "[ \uf07b $path]($style)"
style             = "bold blue"
truncation_length = 4
truncate_to_repo  = false
read_only         = " \uf023"

# ── Git branch (octocat + branch glyph + name) ────────────────────────────────
[git_branch]
format = "[ \uf09b $symbol $branch]($style)"
symbol = "\ue0a0"
style  = "bold green"

# ── Git status (modified/untracked/etc., yellow) ──────────────────────────────
[git_status]
format     = " [$all_status$ahead_behind]($style)"
style      = "bold yellow"
ahead      = "⇡${count} "
behind     = "⇣${count} "
diverged   = "⇕⇡${ahead_count}⇣${behind_count} "
conflicted = "= "
untracked  = "?${count} "
modified   = "!${count} "
staged     = "+${count} "
renamed    = "»${count} "
deleted    = "✘${count} "
stashed    = "≡ "

# ── Dots fill between left and right segments ─────────────────────────────────
[fill]
symbol = "."
style  = "bold black"

# ── User @ host ───────────────────────────────────────────────────────────────
[username]
show_always = true
format      = "[$user]($style)"
style_user  = "bold black"
style_root  = "bold red"

[hostname]
ssh_only = false
format   = "[$hostname]($style)"
style    = "bold blue"

# ── Clock ─────────────────────────────────────────────────────────────────────
[time]
disabled    = false
format      = " [\uf017 $time]($style)"
time_format = "%r"
style       = "bold black"

# ── Misc ──────────────────────────────────────────────────────────────────────
[cmd_duration]
min_time = 500
format   = " [\uf252 $duration]($style)"

[aws]
disabled = true
[gcloud]
disabled = true
[azure]
disabled = true

# ── Palettes ──────────────────────────────────────────────────────────────────
# These redefine the standard colour names used above, so selecting a palette
# (top of file) re-themes the whole prompt. dark = Catppuccin Mocha;
# light = VS Code Light Modern (terminal + light_plus syntax tokens).
[palettes.dark]
blue   = "#89b4fa"
green  = "#a6e3a1"
cyan   = "#89b4fa"
yellow = "#f9e2af"
red    = "#f38ba8"
black  = "#585b70"

[palettes.light]
blue   = "#005fb8"
green  = "#098658"
cyan   = "#005fb8"
yellow = "#949800"
red    = "#CD3131"
black  = "#616161"
{{ end -}}
