{{- /* ~/.tmux.conf — every platform EXCEPT Omarchy, which owns
       ~/.config/tmux/tmux.conf and where tmux prefers that path over this one
       (verified on tmux 3.7c: with both files present the XDG one wins, so this
       file would be applied and then silently ignored). .chezmoiignore gates the
       two against each other; dot_config/tmux/tmux.conf.tmpl is the twin.

       The body is shared through .chezmoitemplates so the two cannot drift. */ -}}
{{ template "tmux-core" . }}
{{ template "tmux-theme" . }}
